{
  "name": "@openlearning/monorepo",
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": {
    "packages": [
      "./packages/*",
      "./apps/*",
      "./frontend",
      "./backend"
    ],
    "catalog": {
      "@sinclair/typebox": "0.34.45",
      "elysia": "^1.4.28"
    }
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.49",
    "elysia": "^1.4.28"
  },
  "overrides": {
    "@sinclair/typebox": "^0.34.49"
  },
  "scripts": {
    "prepare": "husky",
    "build": "bun run --filter=\"*\" --if-present build",
    "dev": "bun run --filter=\"*\" --if-present dev",
    "typecheck": "bun run --filter=\"*\" --if-present typecheck",
    "test": "bun test packages/config packages/domain packages/db apps/worker backend",
    "format": "bun run --filter=\"*\" --if-present format",
    "format:fix": "bun run --filter=\"*\" --if-present format:fix",
    "lint": "bun run --filter=\"*\" --if-present lint",
    "lint:fix": "bun run --filter=\"*\" --if-present lint:fix",
    "db:generate": "bun run --filter=\"*\" --if-present db:generate",
    "db:push": "bun run --filter=\"*\" --if-present db:push",
    "db:seed": "bun run --filter=\"*\" --if-present db:seed",
    "db:studio": "bun run --filter=\"*\" --if-present db:studio",
    "clean": "bun run --filter=\"*\" --if-present clean && rm -rf .turbo node_modules"
  }
}
